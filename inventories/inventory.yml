all:
  hosts:
    rapsberry:
      ansible_host: <IP>
      ansible_port: <PORT>
      ansible_user: <USER>
      ansible_ssh_private_key_file: </PATH/TO/PRIVATE/KEY>
  
  vars:
    ansible_python_interpreter: /usr/bin/python3
